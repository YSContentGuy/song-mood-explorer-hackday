# Copy this file to .env (same directory) and fill in your key
# If OPENAI_API_KEY is not set, the app will run in DEMO MODE and reply locally
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Set this only if you use a different OpenAI-compatible server
# e.g. for local or Azure OpenAI
# OPENAI_BASE_URL=https://api.openai.com/v1